% Generated by roxygen2: do not edit by hand
% Please edit documentation in _main.Rmd.
\name{selected}
\alias{selected}
\alias{selected.cssr}
\title{Extract the selected clusters or features from cluster stability selection}
\usage{
selected(object, ...)

\method{selected}{cssr}(
  object,
  type = c("clusters", "features"),
  cutoff = 0,
  min_num_clusts = 1,
  max_num_clusts = NA,
  weighting = "sparse",
  ...
)
}
\arguments{
\item{object}{An object of class "cssr" (the output of the function \code{\link[=css]{css()}}).}

\item{...}{Additional arguments passed to methods (currently unused).}

\item{type}{Character; either "clusters" (the default) to return the named
list of selected clusters, or "features" to return the flat integer vector of
selected features. May be abbreviated.}

\item{cutoff}{Numeric; only those clusters with selection proportions equal to
at least cutoff will be selected. Must be between 0 and 1. Default is 0 (in
which case either all clusters are selected, or max_num_clusts are selected,
if max_num_clusts is specified).}

\item{min_num_clusts}{Integer or numeric; the minimum number of clusters to
use regardless of cutoff. (That is, if the chosen cutoff returns fewer than
min_num_clusts clusters, the cutoff will be lowered until at least
min_num_clusts clusters are selected.) Default is 1. May be set to 0 to allow
a pure cutoff-based (threshold) selection that returns an empty result when no
cluster's selection proportion meets the cutoff.}

\item{max_num_clusts}{Integer or numeric; the maximum number of clusters to
use regardless of cutoff. (That is, if the chosen cutoff returns more than
max_num_clusts clusters, the cutoff will be raised until at most
max_num_clusts clusters are selected.) Default is NA (in which case
max_num_clusts is ignored).}

\item{weighting}{Character; passed to \code{\link[=getCssSelections]{getCssSelections()}} to determine the
weights of individual features within the selected clusters. This affects
ONLY \code{type = "features"} (it determines which cluster members have nonzero
weight and are therefore returned); it is a no-op for \code{type = "clusters"},
whose selected clusters, selection proportions, and sizes do not depend on the
weighting. Must be one of "sparse", "weighted_avg", or "simple_avg". See
\code{\link[=getCssSelections]{getCssSelections()}} for details. Default is "sparse".}
}
\value{
For the \code{cssr} method: if \code{type = "clusters"} (the default), a named
list of integer vectors (each the indices of the features in one selected
cluster; an empty list if no cluster is selected); if \code{type = "features"}, a
named integer vector of the indices of the selected features (an empty integer
vector if none).
}
\description{
A convenient S3 accessor for the selected clusters (the default) or the
selected features of a fitted \code{cssr} object, without re-running the
(computationally expensive) subsampling. This is a thin wrapper around
\code{\link[=getCssSelections]{getCssSelections()}}.
}
\details{
Note that, unlike the \code{selected()} accessor in the \pkg{stabs} package (which
returns the selected \emph{variables}), the default here returns the selected
\emph{clusters} -- the natural unit of cluster stability selection. Pass
\code{type = "features"} to obtain the flat integer vector of selected features
instead.
}
\examples{
set.seed(1)
data <- genClusteredData(n = 50, p = 11, k_unclustered = 2,
  cluster_size = 4, n_clusters = 1, snr = 3)
clusters <- list(cluster1 = 1:4)
res <- css(X = data$X, y = data$y, lambda = 0.01, clusters = clusters,
  B = 10)
# Selected clusters (the default):
selected(res)
# Selected features (a flat integer vector):
selected(res, type = "features")
}
\references{
Faletto, G., & Bien, J. (2022). Cluster Stability Selection.
\emph{arXiv preprint arXiv:2201.00494}.
\url{https://arxiv.org/abs/2201.00494}.
}
\seealso{
\code{\link[=summary.cssr]{summary.cssr()}} for an overview (counts plus a per-cluster table);
\code{\link[=getCssSelections]{getCssSelections()}} for the underlying selection (clusters, features, and
weights together); \code{\link[=printCssDf]{printCssDf()}} and \code{\link[=print.cssr]{print.cssr()}} for the printed summary.
}
\author{
Gregory Faletto, Jacob Bien
}