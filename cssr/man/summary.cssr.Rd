% Generated by roxygen2: do not edit by hand
% Please edit documentation in _main.Rmd.
\name{print.summary.cssr}
\alias{print.summary.cssr}
\alias{summary.cssr}
\title{Summarize cluster stability selection output}
\usage{
\method{print}{summary.cssr}(x, ...)

\method{summary}{cssr}(
  object,
  cutoff = 0,
  min_num_clusts = 1,
  max_num_clusts = NA,
  weighting = "sparse",
  ...
)
}
\arguments{
\item{x}{An object of class "summary.cssr" (the output of \code{\link[=summary.cssr]{summary.cssr()}}).}

\item{...}{Additional arguments (currently unused).}

\item{object}{An object of class "cssr" (the output of the function \code{\link[=css]{css()}}).}

\item{cutoff}{Numeric; only those clusters with selection proportions equal to
at least cutoff are summarized. Must be between 0 and 1. Default is 0 (in
which case either all clusters are summarized, or max_num_clusts are, if
max_num_clusts is specified).}

\item{min_num_clusts}{Integer or numeric; the minimum number of clusters to
use regardless of cutoff. Default is 1. May be set to 0 to allow a pure
cutoff-based (threshold) selection that can be empty.}

\item{max_num_clusts}{Integer or numeric; the maximum number of clusters to
use regardless of cutoff. Default is NA (in which case max_num_clusts is
ignored).}

\item{weighting}{Character; passed to \code{\link[=getCssSelections]{getCssSelections()}} to determine the
selected features and hence the selected-feature count in the header. As in
\code{\link[=selected]{selected()}}, it does NOT affect which clusters are selected or the
per-cluster table. Must be one of "sparse", "weighted_avg", or "simple_avg".
Default is "sparse".}
}
\value{
An object of class "summary.cssr": a named list with elements
\item{n_selected_clusts}{Integer; the number of selected clusters.}
\item{n_selected_feats}{Integer; the number of selected features.}
\item{cutoff}{Numeric; the cutoff that was used.}
\item{table}{A data.frame with one row per selected cluster (the output of
\code{\link[=printCssDf]{printCssDf()}}), or NULL when the selection is empty.}
\item{pfer}{A placeholder (NA) for per-family error rate error-control
content, to be filled in by a future release (issue #87).}
}
\description{
Produce a concise, computable overview of a fitted \code{cssr} object: a header
with the number of selected clusters and features (at the given cutoff), a
per-cluster table built on top of \code{\link[=printCssDf]{printCssDf()}}, and a placeholder for
per-family error rate (PFER) error-control content.
}
\details{
The returned object has class "summary.cssr" and an accompanying print method
(\code{\link[=print.summary.cssr]{print.summary.cssr()}}). Its \code{table} element is exactly the data.frame
returned by \code{\link[=printCssDf]{printCssDf()}}: one row per selected cluster, sorted by selection
proportion, with columns ClustName, ClustProtoName (only if the features are
named), ClustProtoNum, ClustSelProp (the cluster's selection proportion) and
ClustSize.

If the selection is empty (for example \code{cutoff = 1} with \code{min_num_clusts = 0}
when no cluster reaches the cutoff), \code{summary.cssr} returns a well-formed
zero-row object without error: \code{table} is NULL and the header reports
"0 clusters / 0 features selected". In this case \code{\link[=printCssDf]{printCssDf()}} and its helper
are deliberately NOT called, as they require at least one selected cluster.

The \code{pfer} element is a documented placeholder (NA) reserved for error-control
content to be filled in by a future release (issue #87).
}
\examples{
set.seed(1)
data <- genClusteredData(n = 50, p = 11, k_unclustered = 2,
  cluster_size = 4, n_clusters = 1, snr = 3)
clusters <- list(cluster1 = 1:4)
res <- css(X = data$X, y = data$y, lambda = 0.01, clusters = clusters,
  B = 10)
summary(res)
}
\seealso{
\code{\link[=selected]{selected()}} to extract just the selected clusters or features;
\code{\link[=getCssSelections]{getCssSelections()}} for the underlying selection; \code{\link[=printCssDf]{printCssDf()}} for the
per-cluster data.frame; \code{\link[=print.cssr]{print.cssr()}} for the analogous printed summary.
}
\author{
Gregory Faletto, Jacob Bien
}