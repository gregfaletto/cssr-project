# Helper function to confirm that the argument sampling_type to several functions is as expected

Helper function to confirm that the argument sampling_type to several
functions is as expected

## Usage

``` r
checkSamplingType(sampling_type)
```

## Arguments

- sampling_type:

  A character vector; either "SS" or "MB". "MB" is not supported yet.

## Author

Gregory Faletto, Jacob Bien
